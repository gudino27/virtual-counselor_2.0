version: '3.8'

services:
  # Frontend - React App
  frontend:
    build:
      context: ./virtual-counselor
      dockerfile: Dockerfile
    container_name: virtual-counselor-frontend
    ports:
      - "3007:80"
    environment:
      - NODE_ENV=production
      - REACT_APP_API_URL=http://localhost:3008
    restart: unless-stopped
    depends_on:
      - api
    networks:
      - vc-network

  # Backend API - Node.js + SQLite
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: virtual-counselor-api
    ports:
      - "3008:3008"
    env_file:
      - .env.production
      - prompt-search/.env
    environment:
      - NODE_ENV=production
      - API_PORT=3008
      # WEBHOOK_SECRET is provided via env_file (.env.production)
      # ANTHROPIC_API_KEY and NVIDIA_API_KEY are provided via prompt-search/.env
    volumes:
      - ./data:/app/data
      - ./pdf-archieved-catalog:/app/pdf-archieved-catalog:ro
    restart: unless-stopped
    networks:
      - vc-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3008/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # n8n - Workflow Automation
  n8n:
    image: n8nio/n8n:latest
    container_name: virtual-counselor-n8n
    ports:
      - "3009:5678"
    env_file:
      - .env.production
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      # Provide N8N basic auth credentials via the env_file (.env.production)
      # Add these keys to your .env.production on the host:
      # N8N_BASIC_AUTH_USER=admin
      # N8N_BASIC_AUTH_PASSWORD=<strong-password>
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://api:3008
      - GENERIC_TIMEZONE=America/Los_Angeles
      # WEBHOOK_SECRET is provided via env_file (.env.production)
    volumes:
      - n8n_data:/home/node/.n8n
    restart: unless-stopped
    depends_on:
      - api
    networks:
      - vc-network

  # Local LLM inference — optional, set LOCAL_MODEL_ENABLED=true in .env.production
  # GTX 950 (2GB VRAM): runs Llama 3.2 3B fully on GPU
  llm:
    build:
      context: .
      dockerfile: Dockerfile.llm
      args:
        USE_CUDA: "1"
    container_name: virtual-counselor-llm
    runtime: nvidia
    ports:
      - "8080:8080"
    environment:
      - MODEL_PATH=/models/llama-3.2-3b-instruct-q4_k_m.gguf
      - GPU_LAYERS=28
      - N_CTX=2048
      - N_THREADS=4
      - PORT=8080
    volumes:
      - ./prompt-search/models:/models:ro
    restart: unless-stopped
    networks:
      - vc-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    profiles:
      - local-llm

networks:
  vc-network:
    driver: bridge

volumes:
  n8n_data:
